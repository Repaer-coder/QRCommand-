import Link from 'next/link';import {BarChart3,Building2,Link2,ShieldCheck,Sparkles,Users} from 'lucide-react';
const cells=Array.from({length:81});
export default function Home(){return <><main className="container"><section className="hero"><div><div className="eyebrow">The operating system for every scan</div><h1>Turn every QR code into a measurable business channel.</h1><p>Create branded dynamic codes, update destinations after printing, manage restaurant experiences, and understand exactly when and where customers engage.</p><div className="actions"><Link className="btn" href="/dashboard">Create your first QR</Link><Link className="btn secondary" href="/pricing">View subscriptions</Link></div></div><div className="card preview"><div className="mockqr">{cells.map((_,i)=><i key={i}/>)}</div></div></section><section id="features" className="section"><div className="eyebrow">Premium platform</div><h2>Everything needed to create, control and scale.</h2><p className="sub">Built for independent creators, restaurants, agencies and multi-location companies.</p><div className="grid3">{[[Link2,'Dynamic destinations','Change any destination without replacing the printed code.'],[BarChart3,'Scan intelligence','Track scans, devices, locations, campaigns and conversions.'],[Building2,'Restaurant command center','Menus, ordering, reviews, Wi-Fi, table service and back-of-house workflows.'],[Sparkles,'Brand studio','Custom colors, frames, logos, campaign templates and downloadable assets.'],[Users,'Teams and locations','Role-based access, shared workspaces and multi-location organization.'],[ShieldCheck,'Enterprise protection','Abuse controls, protected links, expiration rules and audit-ready architecture.']].map(([I,t,d])=>{const Icon=I as typeof Link2;return <article className="card feature" key={t as string}><div className="icon"><Icon size={22}/></div><h3>{t as string}</h3><p>{d as string}</p></article>})}</div></section></main><footer className="footer"><div className="container">QR Command · Dynamic QR infrastructure for modern businesses.</div></footer></>}
