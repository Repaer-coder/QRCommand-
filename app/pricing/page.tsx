import Link from 'next/link';
const plans=[['Free','$0','5 QR codes','Static QR codes','PNG downloads'],['Starter','$19','Unlimited static codes','25 dynamic codes','Basic analytics'],['Business','$49','250 dynamic codes','Advanced analytics','Teams + restaurants'],['Enterprise','$149','Unlimited campaigns','White-label domains','API + priority support']];
export default function Pricing(){return <main className="container section"><div className="eyebrow">Simple subscriptions</div><h2>Start free. Upgrade when your scans become business.</h2><p className="sub">Annual billing can be configured in Stripe for discounted rates. All paid plans use secure Stripe Checkout.</p><div className="pricing">{plans.map((p,i)=><article className={`card price ${i===2?'popular':''}`} key={p[0]}>{i===2&&<span className="badge">MOST POPULAR</span>}<h3>{p[0]}</h3><div className="amount">{p[1]}<small>{i?'/month':''}</small></div><div className="list">{p.slice(2).map(x=><span key={x}>✓ {x}</span>)}</div><Link href={`/dashboard?plan=${p[0].toLowerCase()}`} className={`btn ${i===0?'secondary':''}`}>{i===0?'Start free':'Choose plan'}</Link></article>)}</div></main>}
